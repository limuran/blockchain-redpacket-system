{
  "name": "redpacket-contracts",
  "version": "1.0.0",
  "description": "Red Packet Smart Contracts",
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:local": "hardhat run scripts/deploy-redpackage.js --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy-redpackage.js --network sepolia",
    "deploy:mainnet": "hardhat run scripts/deploy-redpackage.js --network mainnet",
    "verify": "hardhat verify",
    "test": "hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^3.0.0",
    "@nomicfoundation/hardhat-verify": "^1.0.0",
    "hardhat": "^2.17.0",
    "dotenv": "^16.0.0"
  },
  "keywords": ["ethereum", "smart-contracts", "redpacket", "dapp"],
  "author": "limuran",
  "license": "MIT"
}
